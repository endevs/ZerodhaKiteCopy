# Pull pre-built images from Docker Hub (no build on server).
# Do NOT pin platform here: on Graviton (ARM64) Docker pulls the linux/arm64 variant from a multi-arch manifest; on x86_64 it pulls amd64 natively.
# Forcing linux/amd64 on ARM caused unstable QEMU and Restarting (255) loops. Push multi-arch from docker-hub-push.ps1 (default platforms).
# Set env before up: export DOCKERHUB_NAMESPACE=yourdockerhubuser IMAGE_TAG=1.0.0
# Requires: backend/.env.production on the server (see backend/.env.production.example)
#
# docker compose -f docker-compose.hub.yml pull
# docker compose -f docker-compose.hub.yml up -d

name: zerodhakite

services:
  backend:
    image: ${DOCKERHUB_NAMESPACE:?set DOCKERHUB_NAMESPACE}/zerodhakite-backend:${IMAGE_TAG:-latest}
    env_file:
      - ./backend/.env.production
    environment:
      TRUST_PROXY: "1"
      SERVER_PORT: "8003"
      SERVER_HOST: "0.0.0.0"
      DATABASE_PATH: /app/data/database.db
    volumes:
      - zerodhakite_data:/app/data
    ports:
      - "8003:8003"
    restart: unless-stopped

  frontend:
    image: ${DOCKERHUB_NAMESPACE:?set DOCKERHUB_NAMESPACE}/zerodhakite-frontend:${IMAGE_TAG:-latest}
    ports:
      - "5175:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  zerodhakite_data:
