# Same as requirements.txt but torch is installed separately in Dockerfile (CPU wheel, ~200MB vs ~2GB+ CUDA).
Flask
# Gunicorn 26 removed the built-in eventlet worker entry point; keep 22.x for Flask-SocketIO + worker_class eventlet (see gunicorn_conf.py)
gunicorn>=22,<23
kiteconnect
Flask-SocketIO
eventlet
APScheduler
flask-cors
pandas
python-dotenv
numpy
scikit-learn
groq
razorpay
cryptography
reportlab
matplotlib
gymnasium>=0.29.0
ollama>=0.1.0
pyotp
playwright
